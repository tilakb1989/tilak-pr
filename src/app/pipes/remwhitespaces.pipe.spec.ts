import { RemwhitespacesPipe } from './remwhitespaces.pipe';

describe('RemwhitespacesPipe', () => {
  it('create an instance', () => {
    const pipe = new RemwhitespacesPipe();
    expect(pipe).toBeTruthy();
  });
});

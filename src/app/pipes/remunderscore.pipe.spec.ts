import { RemunderscorePipe } from './remunderscore.pipe';

describe('RemunderscorePipe', () => {
  it('create an instance', () => {
    const pipe = new RemunderscorePipe();
    expect(pipe).toBeTruthy();
  });
});
